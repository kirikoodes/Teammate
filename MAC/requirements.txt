# TEAMMATE.POTO — Python dependencies for macOS
# Install with:
#   pip3 install -r requirements.txt

numpy>=1.21.0
sounddevice>=0.4.4
mido>=1.2.10
python-rtmidi>=1.4.9
librosa>=0.10.0

# Optional — AI layers
# aubio available on macOS via pip (unlike Windows Python 3.12):
#   pip3 install aubio
# PyTorch CPU:
#   pip3 install torch --index-url https://download.pytorch.org/whl/cpu
